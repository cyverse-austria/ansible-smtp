---
- name: Purge Dovecot
  apt:
    name:
      - dovecot-core
      - dovecot-imapd
      - dovecot-pop3d
    state: absent
    purge: yes

- name: Remove Dovecot directories
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - /var/lib/dovecot
    - /var/log/dovecot.log
